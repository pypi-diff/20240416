# Comparing `tmp/cionuke-0.7.0b4-py2.py3-none-any.whl.zip` & `tmp/cionuke-0.7.1b1-py2.py3-none-any.whl.zip`

## zipinfo {}

```diff
@@ -1,67 +1,64 @@
-Zip file size: 89743 bytes, number of entries: 65
--rw-r--r--  2.0 unx       12 b- defN 24-Feb-29 16:46 cionuke/VERSION
--rw-r--r--  2.0 unx      241 b- defN 24-Feb-29 16:46 cionuke/__about__.py
--rw-r--r--  2.0 unx       24 b- defN 24-Feb-29 16:46 cionuke/__init__.py
--rw-r--r--  2.0 unx     1541 b- defN 24-Feb-29 16:46 cionuke/conductor_menu.py
--rw-r--r--  2.0 unx      671 b- defN 24-Feb-29 16:46 cionuke/const.py
--rw-r--r--  2.0 unx     6101 b- defN 24-Feb-29 16:46 cionuke/copycat_poller.py
--rw-r--r--  2.0 unx     3570 b- defN 24-Feb-29 16:46 cionuke/entry.py
--rw-r--r--  2.0 unx     2593 b- defN 24-Feb-29 16:46 cionuke/node_sync.py
--rw-r--r--  2.0 unx     2154 b- defN 24-Feb-29 16:46 cionuke/post_install.py
--rw-r--r--  2.0 unx    10250 b- defN 24-Feb-29 16:46 cionuke/submit.py
--rw-r--r--  2.0 unx     2452 b- defN 24-Feb-29 16:46 cionuke/utils.py
--rw-r--r--  2.0 unx     3079 b- defN 24-Feb-29 16:46 cionuke/validation.py
--rw-r--r--  2.0 unx        1 b- defN 24-Feb-29 16:46 cionuke/components/__init__.py
--rw-r--r--  2.0 unx     3117 b- defN 24-Feb-29 16:46 cionuke/components/actions.py
--rw-r--r--  2.0 unx     8908 b- defN 24-Feb-29 16:46 cionuke/components/advanced.py
--rw-r--r--  2.0 unx     7713 b- defN 24-Feb-29 16:46 cionuke/components/assets.py
--rw-r--r--  2.0 unx     1155 b- defN 24-Feb-29 16:46 cionuke/components/autosave.py
--rw-r--r--  2.0 unx     1068 b- defN 24-Feb-29 16:46 cionuke/components/conductor_knob.py
--rw-r--r--  2.0 unx     4890 b- defN 24-Feb-29 16:46 cionuke/components/copy_cat.py
--rw-r--r--  2.0 unx     2522 b- defN 24-Feb-29 16:46 cionuke/components/copycat_jobs.py
--rw-r--r--  2.0 unx     3663 b- defN 24-Feb-29 16:46 cionuke/components/environment.py
--rw-r--r--  2.0 unx     3843 b- defN 24-Feb-29 16:46 cionuke/components/frames.py
--rw-r--r--  2.0 unx     2713 b- defN 24-Feb-29 16:46 cionuke/components/instance_type.py
--rw-r--r--  2.0 unx     2473 b- defN 24-Feb-29 16:46 cionuke/components/metadata.py
--rw-r--r--  2.0 unx     3154 b- defN 24-Feb-29 16:46 cionuke/components/preview.py
--rw-r--r--  2.0 unx      768 b- defN 24-Feb-29 16:46 cionuke/components/project.py
--rw-r--r--  2.0 unx     1198 b- defN 24-Feb-29 16:46 cionuke/components/software.py
--rw-r--r--  2.0 unx      593 b- defN 24-Feb-29 16:46 cionuke/components/title.py
--rw-r--r--  2.0 unx      939 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorConnect_18x18.png
--rw-r--r--  2.0 unx     1254 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorConnect_24x24.png
--rw-r--r--  2.0 unx     1815 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorConnect_36x36.png
--rw-r--r--  2.0 unx     2457 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorConnect_48x48.png
--rw-r--r--  2.0 unx     1137 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorError_18x18.png
--rw-r--r--  2.0 unx     1553 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorError_24x24.png
--rw-r--r--  2.0 unx     2265 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorError_36x36.png
--rw-r--r--  2.0 unx     3056 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorError_48x48.png
--rw-r--r--  2.0 unx      748 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorInfo_18x18.png
--rw-r--r--  2.0 unx      892 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorInfo_24x24.png
--rw-r--r--  2.0 unx     1151 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorInfo_36x36.png
--rw-r--r--  2.0 unx     1290 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorInfo_48x48.png
--rw-r--r--  2.0 unx     1047 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorShow_18x18.png
--rw-r--r--  2.0 unx     1392 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorShow_24x24.png
--rw-r--r--  2.0 unx     2160 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorShow_36x36.png
--rw-r--r--  2.0 unx     2874 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorShow_48x48.png
--rw-r--r--  2.0 unx      716 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorSubmit_18x18.png
--rw-r--r--  2.0 unx      886 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorSubmit_24x24.png
--rw-r--r--  2.0 unx     1142 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorSubmit_36x36.png
--rw-r--r--  2.0 unx     1497 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorSubmit_48x48.png
--rw-r--r--  2.0 unx      876 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorSuccess_18x18.png
--rw-r--r--  2.0 unx     1173 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorSuccess_24x24.png
--rw-r--r--  2.0 unx     1552 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorSuccess_36x36.png
--rw-r--r--  2.0 unx     2095 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorSuccess_48x48.png
--rw-r--r--  2.0 unx      932 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorWarning_18x18.png
--rw-r--r--  2.0 unx     1177 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorWarning_24x24.png
--rw-r--r--  2.0 unx     1668 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorWarning_36x36.png
--rw-r--r--  2.0 unx     2204 b- defN 24-Feb-29 16:46 cionuke/icons/ConductorWarning_48x48.png
--rw-r--r--  2.0 unx        1 b- defN 24-Feb-29 16:46 cionuke/widgets/__init__.py
--rw-r--r--  2.0 unx     2067 b- defN 24-Feb-29 16:46 cionuke/widgets/buttoned_scroll_dialog.py
--rw-r--r--  2.0 unx     8037 b- defN 24-Feb-29 16:46 cionuke/widgets/key_value_grp.py
--rw-r--r--  2.0 unx     1729 b- defN 24-Feb-29 16:46 cionuke/widgets/notice_grp.py
--rw-r--r--  2.0 unx     1288 b- defN 24-Feb-29 16:46 cionuke-0.7.0b4.data/data/nuke/menu.py
--rw-r--r--  2.0 unx     5406 b- defN 24-Feb-29 16:46 cionuke-0.7.0b4.dist-info/METADATA
--rw-r--r--  2.0 unx      110 b- defN 24-Feb-29 16:46 cionuke-0.7.0b4.dist-info/WHEEL
--rw-r--r--  2.0 unx        8 b- defN 24-Feb-29 16:46 cionuke-0.7.0b4.dist-info/top_level.txt
--rw-rw-r--  2.0 unx     5735 b- defN 24-Feb-29 16:46 cionuke-0.7.0b4.dist-info/RECORD
-65 files, 146796 bytes uncompressed, 80565 bytes compressed:  45.1%
+Zip file size: 84524 bytes, number of entries: 62
+-rw-r--r--  2.0 unx       12 b- defN 24-Apr-16 15:47 cionuke/VERSION
+-rw-r--r--  2.0 unx      241 b- defN 24-Apr-16 15:46 cionuke/__about__.py
+-rw-r--r--  2.0 unx       24 b- defN 24-Apr-16 15:46 cionuke/__init__.py
+-rw-r--r--  2.0 unx     1541 b- defN 24-Apr-16 15:46 cionuke/conductor_menu.py
+-rw-r--r--  2.0 unx     1189 b- defN 24-Apr-16 15:46 cionuke/connect.py
+-rw-r--r--  2.0 unx      477 b- defN 24-Apr-16 15:46 cionuke/const.py
+-rw-r--r--  2.0 unx     3350 b- defN 24-Apr-16 15:46 cionuke/entry.py
+-rw-r--r--  2.0 unx     2154 b- defN 24-Apr-16 15:46 cionuke/post_install.py
+-rw-r--r--  2.0 unx     8964 b- defN 24-Apr-16 15:46 cionuke/submit.py
+-rw-r--r--  2.0 unx     2452 b- defN 24-Apr-16 15:46 cionuke/utils.py
+-rw-r--r--  2.0 unx     5481 b- defN 24-Apr-16 15:46 cionuke/validation.py
+-rw-r--r--  2.0 unx        1 b- defN 24-Apr-16 15:46 cionuke/components/__init__.py
+-rw-r--r--  2.0 unx     2183 b- defN 24-Apr-16 15:46 cionuke/components/actions.py
+-rw-r--r--  2.0 unx     7014 b- defN 24-Apr-16 15:46 cionuke/components/advanced.py
+-rw-r--r--  2.0 unx     7998 b- defN 24-Apr-16 15:46 cionuke/components/assets.py
+-rw-r--r--  2.0 unx     1155 b- defN 24-Apr-16 15:46 cionuke/components/autosave.py
+-rw-r--r--  2.0 unx     1068 b- defN 24-Apr-16 15:46 cionuke/components/conductor_knob.py
+-rw-r--r--  2.0 unx     3663 b- defN 24-Apr-16 15:46 cionuke/components/environment.py
+-rw-r--r--  2.0 unx     3446 b- defN 24-Apr-16 15:46 cionuke/components/frames.py
+-rw-r--r--  2.0 unx     2713 b- defN 24-Apr-16 15:46 cionuke/components/instance_type.py
+-rw-r--r--  2.0 unx     2473 b- defN 24-Apr-16 15:46 cionuke/components/metadata.py
+-rw-r--r--  2.0 unx     3154 b- defN 24-Apr-16 15:46 cionuke/components/preview.py
+-rw-r--r--  2.0 unx      768 b- defN 24-Apr-16 15:46 cionuke/components/project.py
+-rw-r--r--  2.0 unx     3629 b- defN 24-Apr-16 15:46 cionuke/components/software.py
+-rw-r--r--  2.0 unx      593 b- defN 24-Apr-16 15:46 cionuke/components/title.py
+-rw-r--r--  2.0 unx      939 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorConnect_18x18.png
+-rw-r--r--  2.0 unx     1254 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorConnect_24x24.png
+-rw-r--r--  2.0 unx     1815 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorConnect_36x36.png
+-rw-r--r--  2.0 unx     2457 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorConnect_48x48.png
+-rw-r--r--  2.0 unx     1137 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorError_18x18.png
+-rw-r--r--  2.0 unx     1553 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorError_24x24.png
+-rw-r--r--  2.0 unx     2265 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorError_36x36.png
+-rw-r--r--  2.0 unx     3056 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorError_48x48.png
+-rw-r--r--  2.0 unx      748 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorInfo_18x18.png
+-rw-r--r--  2.0 unx      892 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorInfo_24x24.png
+-rw-r--r--  2.0 unx     1151 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorInfo_36x36.png
+-rw-r--r--  2.0 unx     1290 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorInfo_48x48.png
+-rw-r--r--  2.0 unx     1047 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorShow_18x18.png
+-rw-r--r--  2.0 unx     1392 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorShow_24x24.png
+-rw-r--r--  2.0 unx     2160 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorShow_36x36.png
+-rw-r--r--  2.0 unx     2874 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorShow_48x48.png
+-rw-r--r--  2.0 unx      716 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorSubmit_18x18.png
+-rw-r--r--  2.0 unx      886 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorSubmit_24x24.png
+-rw-r--r--  2.0 unx     1142 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorSubmit_36x36.png
+-rw-r--r--  2.0 unx     1497 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorSubmit_48x48.png
+-rw-r--r--  2.0 unx      876 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorSuccess_18x18.png
+-rw-r--r--  2.0 unx     1173 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorSuccess_24x24.png
+-rw-r--r--  2.0 unx     1552 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorSuccess_36x36.png
+-rw-r--r--  2.0 unx     2095 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorSuccess_48x48.png
+-rw-r--r--  2.0 unx      932 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorWarning_18x18.png
+-rw-r--r--  2.0 unx     1177 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorWarning_24x24.png
+-rw-r--r--  2.0 unx     1668 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorWarning_36x36.png
+-rw-r--r--  2.0 unx     2204 b- defN 24-Apr-16 15:47 cionuke/icons/ConductorWarning_48x48.png
+-rw-r--r--  2.0 unx        1 b- defN 24-Apr-16 15:46 cionuke/widgets/__init__.py
+-rw-r--r--  2.0 unx     2067 b- defN 24-Apr-16 15:46 cionuke/widgets/buttoned_scroll_dialog.py
+-rw-r--r--  2.0 unx     8037 b- defN 24-Apr-16 15:46 cionuke/widgets/key_value_grp.py
+-rw-r--r--  2.0 unx     1729 b- defN 24-Apr-16 15:46 cionuke/widgets/notice_grp.py
+-rw-r--r--  2.0 unx     1288 b- defN 24-Apr-16 15:46 cionuke-0.7.1b1.data/data/nuke/menu.py
+-rw-r--r--  2.0 unx     5519 b- defN 24-Apr-16 15:47 cionuke-0.7.1b1.dist-info/METADATA
+-rw-r--r--  2.0 unx      110 b- defN 24-Apr-16 15:47 cionuke-0.7.1b1.dist-info/WHEEL
+-rw-r--r--  2.0 unx        8 b- defN 24-Apr-16 15:47 cionuke-0.7.1b1.dist-info/top_level.txt
+-rw-rw-r--  2.0 unx     5472 b- defN 24-Apr-16 15:47 cionuke-0.7.1b1.dist-info/RECORD
+62 files, 131922 bytes uncompressed, 75756 bytes compressed:  42.6%
```

## zipnote {}

```diff
@@ -6,26 +6,23 @@
 
 Filename: cionuke/__init__.py
 Comment: 
 
 Filename: cionuke/conductor_menu.py
 Comment: 
 
-Filename: cionuke/const.py
+Filename: cionuke/connect.py
 Comment: 
 
-Filename: cionuke/copycat_poller.py
+Filename: cionuke/const.py
 Comment: 
 
 Filename: cionuke/entry.py
 Comment: 
 
-Filename: cionuke/node_sync.py
-Comment: 
-
 Filename: cionuke/post_install.py
 Comment: 
 
 Filename: cionuke/submit.py
 Comment: 
 
 Filename: cionuke/utils.py
@@ -48,20 +45,14 @@
 
 Filename: cionuke/components/autosave.py
 Comment: 
 
 Filename: cionuke/components/conductor_knob.py
 Comment: 
 
-Filename: cionuke/components/copy_cat.py
-Comment: 
-
-Filename: cionuke/components/copycat_jobs.py
-Comment: 
-
 Filename: cionuke/components/environment.py
 Comment: 
 
 Filename: cionuke/components/frames.py
 Comment: 
 
 Filename: cionuke/components/instance_type.py
@@ -174,23 +165,23 @@
 
 Filename: cionuke/widgets/key_value_grp.py
 Comment: 
 
 Filename: cionuke/widgets/notice_grp.py
 Comment: 
 
-Filename: cionuke-0.7.0b4.data/data/nuke/menu.py
+Filename: cionuke-0.7.1b1.data/data/nuke/menu.py
 Comment: 
 
-Filename: cionuke-0.7.0b4.dist-info/METADATA
+Filename: cionuke-0.7.1b1.dist-info/METADATA
 Comment: 
 
-Filename: cionuke-0.7.0b4.dist-info/WHEEL
+Filename: cionuke-0.7.1b1.dist-info/WHEEL
 Comment: 
 
-Filename: cionuke-0.7.0b4.dist-info/top_level.txt
+Filename: cionuke-0.7.1b1.dist-info/top_level.txt
 Comment: 
 
-Filename: cionuke-0.7.0b4.dist-info/RECORD
+Filename: cionuke-0.7.1b1.dist-info/RECORD
 Comment: 
 
 Zip file comment:
```

## cionuke/VERSION

```diff
@@ -1 +1 @@
-0.7.0-beta.4
+0.7.1-beta.1
```

## cionuke/const.py

```diff
@@ -6,16 +6,12 @@
 FEATURE_DEV = os.environ.get("CIO_FEATURE_DEV", False)
 
 VERSION = "dev.999"
 PLUGIN_DIR = os.path.dirname(__file__)
 
 CONDUCTOR_COMMAND_PATH = os.path.join(os.path.dirname(PLUGIN_DIR), "bin", "conductor")
 
-COPY_CAT_MODES = {'single_instance': 'Single Instance',
-                  'distributed_local': 'Distributed (local Main)',
-                  'distributed_remote':  'Distributed (remote Main)'}
-
 try:
     with open(os.path.join(PLUGIN_DIR, "VERSION")) as version_file:
         VERSION = version_file.read().strip()
 except BaseException:
     pass
```

## cionuke/entry.py

```diff
@@ -2,20 +2,19 @@
 
 import datetime
 import os
 import sys
 
 import nuke
 
-from cionuke import utils
+from cionuke import utils, connect
 from cionuke.components import (
     actions,
     advanced,
     autosave,
-    copy_cat,
     environment,
     assets,
     frames,
     instance_type,
     metadata,
     preview,
     project,
@@ -39,44 +38,41 @@
     Add a Conductor submitter node to the project.
 
     Connect selected Write nodes to the selected submitter nodes.
     If no submitter exists, build a new one.
     """
 
     submitters = [n for n in nuke.selectedNodes("Group") if n.knob("cio_title")]
+    write_nodes = [n for n in nuke.selectedNodes("Write")]
 
-    if nuke.selectedNodes("Write") and nuke.selectedNodes("CopyCat"):
-        raise Exception("Write nodes and CopyCat nodes have both been selected. Please only select one type.")
-    
-    else:
-        # We can query for both node types since only a single node type can be selected
-        render_nodes = [n for n in (nuke.selectedNodes("Write") + nuke.selectedNodes("CopyCat"))]
-
-    if not render_nodes:
+    if not write_nodes:
         print("Select at least one Write node.")
         return
 
     build_new = False
     if not submitters:
         build_new = True
         submitter = nuke.createNode("Group")
         submitter.setName("Conductor")
+        build_tabs(submitter)
         submitters = [submitter]
+        connect.connect(submitter, force=False)
+        software.detect_version(submitter)
 
+    # connect write nodes to submitters
     for submitter in submitters:
         inputs = submitter.dependencies(nuke.INPUTS | nuke.HIDDEN_INPUTS)
         i = len(inputs)
-        for render_node in render_nodes:
-            if render_node not in inputs:
-                render_node.knob("selected").setValue(False)
-                submitter.setInput(i, render_node)
+        for write_node in write_nodes:
+            if write_node not in inputs:
+                write_node.knob("selected").setValue(False)
+                submitter.setInput(i, write_node)
                 i += 1
 
     if build_new:
-        build_tabs(submitter)
         submitter.knob("selected").setValue(True)
         nuke.show(submitter)
         submitter.knob("Configure").setFlag(0)
 
 
 def build_tabs(submitter):
     """
@@ -91,21 +87,15 @@
     actions.build(submitter)
     title.build(submitter)
     project.build(submitter)
     instance_type.build(submitter)
     utils.divider(submitter, "div_1")
     software.build(submitter)
     utils.divider(submitter, "div_2")
-
-    if submitter.input(0).Class() == "CopyCat":
-        copy_cat.build(submitter)
-
-    else:
-        frames.build(submitter)
-    
+    frames.build(submitter)
     utils.divider(submitter, "div_3")
     advanced.build(submitter)
     environment.build(submitter)
     utils.divider(submitter, "div_4")
     assets.build(submitter)
     utils.divider(submitter, "div_5")
     metadata.build(submitter)
@@ -114,28 +104,31 @@
 
     preview_tab = nuke.Tab_Knob("Preview")
     submitter.addKnob(preview_tab)
     preview.build(submitter)
 
     submitter.knob("knobChanged").setValue(SAFE_KNOB_CHANGED)
 
+    # Trigger a preview update now
+    preview.knobChanged(submitter, submitter.knob("cio_title"))
+
+
 COMPONENTS = (
     actions,
     project,
     title,
     instance_type,
     software,
     environment,
     metadata,
     assets,
     frames,
     advanced,
     autosave,
     preview,
-    copy_cat
 )
 
 def knobChanged():
     """
     Notify all component modules when a knob changes.
     """
     node = nuke.thisNode()
```

## cionuke/submit.py

```diff
@@ -1,23 +1,17 @@
 """
 Handle the submission.
 """
 
-import sys
-
 from contextlib import contextmanager
  
-import time
 from PySide2 import QtWidgets 
-import threading
 import nuke
 from ciocore import conductor_submit
-import traceback
 from ciocore import config
-import json
 from cionuke import const as k
 
 
 
 try:
     import urlparse as parse
 except ImportError:
@@ -34,16 +28,14 @@
     instance_type,
     software,
     environment,
     metadata,
     assets,
     frames,
     advanced,
-    copy_cat,
-    copycat_jobs
 )
 
 # NOTE: The order is important!
 # assets comes first because dependency scraping might be expensive.
 # We need the results of dependency scraping in order to form the
 # windows path mapping args, which are used by the frames component,
 # where tasks are generated.
@@ -52,15 +44,14 @@
     project,
     title,
     instance_type,
     software,
     environment,
     metadata,
     frames,
-    copy_cat,
     advanced,
 )
 
 
 HEADER_ERROR = [
     "There are issues that would cause your submission to fail.",
     "To save you unwanted costs, the submission is blocked. ",
@@ -99,30 +90,30 @@
 
 @contextmanager
 def transient_save(submitter):
     """
     Save with the autosave name for submission, then revert.
     """
     cio_filename = submitter.knob("cio_autosave_template").getValue()
+    original = nuke.Root().knob("name").getValue()
     try:
-        original = nuke.Root().knob("name").getValue()
         nuke.scriptSaveAs(cio_filename, overwrite=1)
         yield
     except OSError:
         print("Problem saving nuke script")
     finally:
         nuke.Root().knob("name").setValue(original)
 
 
 class SubmissionDialog(ButtonedScrollDialog):
     def __init__(self, submitter, submitting, messages):
         self.submitting = submitting
-        title = "Submission" if submitting else "Validation"
+        submission_title = "Submission" if submitting else "Validation"
         super(SubmissionDialog, self).__init__(
-            submitter, title=title, buttons=[("cancel", "Close"), ("continue", "Continue")]
+            submitter, title=submission_title, buttons=[("cancel", "Close"), ("continue", "Continue")]
         )
 
         self.resize(600 * self.screen_scale, 800 * self.screen_scale)
         self.buttons["cancel"].clicked.connect(self.reject)
         self.populate_validation_messages(messages)
 
     def populate_validation_messages(self, messages):
@@ -166,40 +157,20 @@
         else:
             self.buttons["continue"].setVisible(False)
 
     def continue_submission(self):
 
         try:
             response, response_code = self.save_and_submit()
-            print(response)
-            response_code = 200
         except BaseException as ex:
             response = {"body": str(ex)}
         if not response:
             response = {"body": "Submission cancelled."}
         self.handle_response(response)
 
-        # Only if the job was for distributed remote copycat
-        if (response_code == 200 and 
-            self.submitter.input(0).Class() == "CopyCat" and
-            self.submitter.knob("cio_copycat_mode").value() == k.COPY_CAT_MODES['distributed_remote']
-        ):
-            
-            jid = response['jobid']
-            
-            print("Adding jobs tab")
-            copycat_jobs.build(self.submitter, jid=jid)
-
-        else:
-            print("ugh")
-            print(self.submitter.input(0).Class())
-            print(self.submitter.knob("cio_copycat_mode").value())
-            
-
-
     def save_and_submit(self):
         """
         Save or autosave, then submit
 
         Returns:
             tuple: submission response and code.
         """
@@ -302,23 +273,9 @@
 
     Returns:
         dict: payload, including tasks, assets, project, and so on
     """
     submission = {}
 
     for component in COMPONENTS:
-        result = component.resolve(submitter, **kwargs)
-        
-        for key, value in result.items():
-            if key not in submission:
-                submission[key] = value
-            else:
-
-                if isinstance(submission[key], dict):
-                    submission[key].update(value)
-
-                elif isinstance(submission[key], list):
-                    submission[key].extend(value)
-
-                else:
-                    raise Exception("Unable to update submission element '{}'. Trying to replace value '{}' with '{}'".format(key, submission[key], value))
+        submission.update(component.resolve(submitter, **kwargs))
     return submission
```

## cionuke/validation.py

```diff
@@ -3,68 +3,130 @@
 """
 
 import os
 import sys
 
 from ciocore.validator import Validator
 from cionuke import const as k
-from cionuke.components import frames
-from cionuke import submit
+from cionuke.components import frames,  advanced, assets
+from cionuke import connect
+
+from ciocore import data as coredata
 
 
 import json
 
-class ValidateTaskCount(Validator):
+
+class ValidateConductorData(Validator):
+
     def run(self, _):
+        """
+        This validator checks that the Conductor data is valid and has not become outdated.
+
+        It is required because the user may have opened an old scene file pointing to resources that are no longer available on Conductor.
+
+        It remmbers existing values for project, instance type, and Nuke version. It then connects to Conductor (which updates the selected values) and checks to see if the available values have changed. If they have, it warns the user. It does not block the submission, because the changed resource may be perfectly acceptable. Example: Trojan War => Troy, or Nuke14.0 => Nuke14.5
+
+        If for some reason after connecting, the data is still invalid, it will block the submission.
+
+        No messages are generated if all the data is valid and did not change after a connect.
+        """
+
+        invalid_msg = "Conductor data is not valid. Please press the Connect button and check the configuration for Project, Instance type, and Nuke version before you re-submit."
 
-        if self._submitter.input(0).Class == "Write":
-            main_seq, scout_seq = frames.get_sequences(self._submitter)
+        orig_project = self._submitter.knob("cio_project").value()
+        orig_insttype = self._submitter.knob("cio_insttype").value()
+        orig_software = self._submitter.knob("cio_software").value()
 
-            count = len(main_seq.chunks())
-            if count > 1000:
-                self.add_warning(
-                    "This submission contains over 1000 tasks ({}). Are you sure this is correct?".format(
-                        count
-                    )
+        connect.connect(self._submitter)
+
+        project = self._submitter.knob("cio_project").value()
+        insttype = self._submitter.knob("cio_insttype").value()
+        software = self._submitter.knob("cio_software").value()
+
+        if (
+            not coredata.valid()
+            or project == k.NOT_CONNECTED
+            or insttype == k.NOT_CONNECTED
+            or software == k.NOT_CONNECTED
+        ):
+            self.add_error(invalid_msg)
+            return
+
+        template = "Available {resource}s on Conductor may have changed since you last set the value: {orig_value} -> {new_value}. If {new_value} is incorrect, close this panel and choose the correct {resource}."
+
+        msgs = []
+        if project != orig_project:
+            msgs.append(
+                template.format(
+                    resource="project", orig_value=orig_project, new_value=project
                 )
+            )
 
-class ValidateOutputPath(Validator):
+        if insttype != orig_insttype:
+            msgs.append(
+                template.format(
+                    resource="instance type",
+                    orig_value=orig_insttype,
+                    new_value=insttype,
+                )
+            )
+
+        if software != orig_software:
+            msgs.append(
+                template.format(
+                    resource="Nuke version",
+                    orig_value=orig_software,
+                    new_value=software,
+                )
+            )
+
+        if msgs:
+            self.add_warning("\n\n".join(msgs))
+
+
+# TODO: Implement auto chunking for over k.MAX_TASK_COUNT frames. Then we'll be able to remove this validator. Check Maya submitter for reference.
+class ValidateTaskCount(Validator):
     def run(self, _):
+        main_seq, scout_seq = frames.get_sequences(self._submitter)
 
-        submission = submit.resolve_submission(submitter=self._submitter, should_scrape_assets=True)
-        output_path = submission['output_path']
+        count = len(main_seq.chunks())
+        if count > 1000:
+            self.add_warning(
+                "This submission contains over 1000 tasks ({}). Are you sure this is correct?".format(
+                    count
+                )
+            )
 
+class ValidateOutputPath(Validator):
+    def run(self, _):
+        
+        output_path = advanced.evaluate_output_path(self._submitter)
+            
+ 
         if not output_path:
             self.add_error("The Conductor output path has not been set. Please ensure the the file knob on your Write nodes is properly set.")
             return
 
         invalid_asset_paths = []
-  
-        for p in submission['upload_paths']:
+
+        output_paths = assets.resolve(self._submitter, should_scrape_assets=True)
+            
+        for p in output_paths:
 
             if os.path.abspath(p).startswith(os.path.abspath(output_path)):
                 invalid_asset_paths.append(p)
 
         if invalid_asset_paths:
             self.add_error(
-                "This submission contains dependencies that are located within the output path ({}). The output path can not be the root of any files being uploaded as part of this job.\n\nPlease change the output path to a more specific folder.\n\n\t{}".format(
+                "This submission contains dependencies that are located within the output path ({}). The output path can not be the root of any files being uploaded as part of this job.\n\nPlease change the output path to a more specific folder. Alternatively, relocate the offending dependencies.\n\n\t{}".format(
                     output_path, "\n\t".join(invalid_asset_paths)
                 )
             )
 
-class ValidateCopyCatPlatform(Validator):
-    def run(self, _):
-
-        # Submitting a distributed job where the main node is local is only support if the submitting machine is linux.
-        if ( self._submitter.input(0).Class == "CopyCat" and
-             self._submitter.knob("cio_copycat_mode").value() == k.COPY_CAT_MODES['distributed_local'] and
-             sys.platform != 'linux' ):
-            
-            self.add_error("Submitting CopyCat jobs with the mode {} are only supported if the submitting machine is running linux.".format(k.COPY_CAT_MODES['distributed_local']))
-
 def run(submitter):
 
     meta_warnings = set()
 
     validators = [plugin(submitter) for plugin in Validator.plugins()]
 
     for validator in validators:
```

## cionuke/components/actions.py

```diff
@@ -1,22 +1,17 @@
 """
 Row of action buttons at the top of the submitter.
 
 Currently, Connect, Validate, Submit
 """
 import os
-
 import nuke
-from ciocore import data as coredata
-
-
 import cionuke
-from cionuke import const as k
-from cionuke import submit
-from cionuke.components import instance_type, project, software
+from cionuke import submit, connect
+ 
 
 # In development, you can use fixtures for packages, projects, instance types.
 # It saves time. Fixtures
 FIXTURES_DIR = os.path.expanduser(os.path.join("~", "conductor_fixtures"))
  
 CONNECT_TOOLTIP = """
 Connects to your acccount on Conductor. You may be asked to log in. 
@@ -57,45 +52,23 @@
     Respond to button pushes.
 
     Submit has a validate step before submission. 
     submitting=False causes the operation to stop after validation. 
     """
     knob_name = knob.name()
     if knob_name == "cio_connect":
-        connect(node)
+        connect.connect(node)
     elif knob_name == "cio_validate":
         submit.submit(node, submitting=False)
     elif knob_name == "cio_submit":
         submit.submit(node, submitting=True)
 
 
 def affector_knobs():
     """
     Knobs in this component that affect the payload when changed.
 
     If a write node is added, or if the connect button is pressed, the payload needs to be updated.
     """
 
     return ["inputChange", "cio_connect"]
-
-def connect(submitter):
-    """
-    Connect to Conductor in order to access users account data.
-
-    Menus must be repopulated.
-    """
-
-    coredata.init(product="nuke")
-    if k.FEATURE_DEV:
-        use_fixtures = submitter.knob("cio_use_fixtures").getValue()
-        fixtures_dir = FIXTURES_DIR if use_fixtures else None
-        coredata.set_fixtures_dir(fixtures_dir)
-
-    try:
-        coredata.data(force=True)
-    except BaseException as ex:
-        print(str(ex))
-        print("Try again after deleting your credentials file (~/.config/conductor/credentials)")
-    if coredata.valid():
-        project.rebuild_menu(submitter, coredata.data()["projects"])
-        instance_type.rebuild_menu(submitter, coredata.data()["instance_types"])
-        software.rebuild_menu(submitter, coredata.data().get("software"))
+
```

## cionuke/components/advanced.py

```diff
@@ -10,19 +10,16 @@
 from cionuke import utils
 from cionuke import const as k
 
 from ciopath.gpath_list import PathList
 
 LOG = ciocore.loggeria.get_conductor_logger()
 
-DEFAULT_TASK_TEMPLATES = {'Write': 'nuke -V 2 --remap "[value cio_pathmap]" -F [value cio_range_args] -X [python {",".join([n.name() for n in nuke.thisNode().dependencies(nuke.INPUTS | nuke.HIDDEN_INPUTS)])}] "[regsub -nocase {^[A-Z]:} [value root.name] []]"',
-                          'CopyCat': 'nuke -V 2 --gpu --multigpu --remap "[value cio_pathmap]" -F 1 -X [python {",".join([n.name() for n in nuke.thisNode().dependencies(nuke.INPUTS | nuke.HIDDEN_INPUTS)])}] "[regsub -nocase {^[A-Z]:} [value root.name] []]"'
-                         }
+DEFAULT_TASK_TEMPLATE = 'nuke -V 2 --remap "[value cio_pathmap]" -F [value cio_range_args] -X [python {",".join([n.name() for n in nuke.thisNode().dependencies(nuke.INPUTS | nuke.HIDDEN_INPUTS)])}] "[regsub -nocase {^[A-Z]:} [value root.name] []]"'
 
-#"command": "python3 /Users/j_leh/OneDrive/Documents/vs_workspace/cio_client_debug/nuke/copycat_scripts/node_sync.py; bash /Users/j_leh/OneDrive/Documents/vs_workspace/cio_client_debug/nuke/copycat_scripts/${AGENT_DNS_RECORD}.sh; sleep 3600s",
 
 USE_DAEMON_TOOLTIP = """
 An upload daemon is a separate background process that uploads files outside of Nuke. It means the 
 submission in Nuke is not blocked while assets are uploaded. If you turn this option on, you'll need
 to run conductor uploader in a terminal window / command prompt.
 """
 
@@ -82,18 +79,17 @@
 
     knob = nuke.Int_Knob("cio_retries", "Retries on preemption")
     knob.setValue(1)
     knob.setTooltip(RETRIES_TOOLTIP)
     knob.setFlag(nuke.STARTLINE)
     submitter.addKnob(knob)
 
-    task_template = DEFAULT_TASK_TEMPLATES[submitter.input(0).Class()]
-    nuke.knobDefault("Group.cio_task", task_template)
+    nuke.knobDefault("Group.cio_task", DEFAULT_TASK_TEMPLATE)
     knob = nuke.EvalString_Knob("cio_task", "Task template")
-    knob.setValue(task_template)
+    knob.setValue(DEFAULT_TASK_TEMPLATE)
     knob.setTooltip(TASK_TOOLTIP)
     submitter.addKnob(knob)
     knob.setVisible(False)
 
     knob = nuke.Boolean_Knob("cio_use_custom_task", "Use custom task")
     knob.clearFlag(nuke.STARTLINE)
     knob.setTooltip(USE_CUSTOM_TASK_TOOLTIP)
@@ -130,15 +126,15 @@
         node.knob("cio_emails").setEnabled(use_emails)
 
         use_custom_task = bool(node.knob("cio_use_custom_task").getValue())
         node.knob("cio_task").setVisible(use_custom_task)
 
         # reset the task expression when switching off use_custom
         if not use_custom_task:
-            node.knob("cio_task").setValue(DEFAULT_TASK_TEMPLATES[node.input(0).Class()])
+            node.knob("cio_task").setValue(DEFAULT_TASK_TEMPLATE)
 
 
 def resolve(submitter, **kwargs):
     """
     Resolve the part of the payload that is handled by this component.
     """
     result = {}
@@ -149,29 +145,14 @@
     if location:
         result["location"] = location
 
     if submitter.knob("cio_use_emails").getValue():
         emails = list([_f for _f in re.split(r"[, ]+", submitter.knob("cio_emails").getValue()) if _f])
         if emails:
             result["notify"] = emails
-    
-    if not bool(submitter.knob("cio_use_custom_task").getValue()):
-        if submitter.input(0).Class == "CopyCat":
-
-            if submitter.knob("cio_copycat_mode").value() == k.COPY_CAT_MODES['distributed_local']:
-                task_cmd = "COPYCAT_MAIN_ADDR={ip} COPYCAT_MAIN_PORT={port} COPYCAT_RANK={rank} COPYCAT_WORLD_SIZE={worker_size} {cmd}".format(ip="184.162.127.134",
-                                                                                                                               port="30000",
-                                                                                                                               rank="1",
-                                                                                                                               worker_size="2",
-                                                                                                                               cmd=task_cmd)
-                print("Updating the task cmd with: {}".format(task_cmd))
-                submitter.knob('cio_task').setValue(task_cmd)                
-
-
-    
     try:
         result["output_path"] = evaluate_output_path(submitter)
     except BaseException as err_msg:
         LOG.warning(err_msg)
         result["output_path"] = ""
     return  result
 
@@ -180,26 +161,17 @@
     """
     Output path is calculated as the common ancestor path of connected Write nodes. 
 
     Returns:
         [string]: A path
     """
     path_list = PathList()
-    for node in submitter.dependencies():
-
-        if node.Class() == "Write":
-            path_knob = node.knob("file")
-
-        elif node.Class() == "CopyCat":
-            path_knob = node.knobs()['dataDirectory']
-
-        else:
-            continue
+    for node in [n for n in submitter.dependencies() if n.Class() == "Write"]:
 
-        value = utils.as_globbable_path(path_knob)
+        value = utils.as_globbable_path(node.knob("file"))
 
         if not value:
             raise Exception("The Write node '%s' doesn't have the file knob set." % node.name())
 
         value = os.path.dirname(value)
         value = utils.truncate_path_to_star(value)
         if value:
```

## cionuke/components/assets.py

```diff
@@ -11,15 +11,15 @@
 
 LOG = ciocore.loggeria.get_conductor_logger()
 
 from cionuke.components.conductor_knob import ConductorKnob
 from ciopath.gpath_list import PathList
 from cionuke import utils
 
-NODE_BLACKLIST = ["Write", "DeepWrite", "CopyCat"]
+NODE_BLACKLIST = ["Write", "DeepWrite"]
 
 ASSETS_TOOLTIP = """
 In the event tat some assets are not picked up by the dependency scraper, you may add them here.
 To see what assets are found by the dependency scraper, go to the preview panel and press the show assets button. 
 """
 
 
@@ -146,33 +146,39 @@
     knob = nuke.String_Knob("cio_pathmap", "Pathmap args")
     submitter.addKnob(knob)
     knob.setVisible(False)
 
 def resolve(submitter, **kwargs):
     """
     Resolve the part of the payload that is handled by this component.
+    
+    1. Get the extra assets. (Assets manually specified by the user)
+    2. Scrape the node graph for assets.
+    3. The real_files() call resolves to files on disk. It expands folders, expands globs, and removes missing files.
+    
+    We also set up the pathmap args here.
+    TODO: Skip pathmap on Mac and Linux.
     """
 
     should_scrape_assets = kwargs.get("should_scrape_assets")
     extra_assets = json.loads(submitter.knob("cio_extra_assets").getText() or "[]")
 
     scraped_assets = scrape_assets(submitter) if should_scrape_assets else []
     path_list = PathList()
     path_list.add(*extra_assets)
     path_list.add(*scraped_assets)
+    path_list.real_files()
     script  = nuke.Root().knob("name").getValue().strip()
     if not script:
         nuke.alert("No script name in Project Settings! Has this script been saved?")
     else:
         path_list.add(script)
 
     set_pathmap_args(submitter, path_list)
 
-    path_list.remove_missing()
-    path_list.glob()
     return {"upload_paths": sorted([p.fslash() for p in path_list])}
 
 def set_pathmap_args(submitter, path_list):
     """Set up a drive letter remapping string for Windows."""
     prefix_set = set()
 
     for p in path_list:
```

## cionuke/components/frames.py

```diff
@@ -1,14 +1,15 @@
 import nuke
 
 from cioseq.sequence import Sequence
 import re
 
 SCOUT_AUTO_REGEX = re.compile(r"^auto[, :]+(\d+)$")
 
+
 def build(submitter):
     """Build knobs to specify frame ranges."""
     knob = nuke.Int_Knob("cio_chunk_size", "Chunk size")
     knob.setValue(1)
     submitter.addKnob(knob)
 
     knob = nuke.String_Knob("cio_custom_frames", "Custom frames", "1-10")
@@ -25,63 +26,53 @@
     knob = nuke.Boolean_Knob("cio_use_scout_tasks", "Use scout tasks    ")
     knob.clearFlag(nuke.STARTLINE)
     knob.setValue(True)
     submitter.addKnob(knob)
 
     knobChanged(submitter, submitter.knob("cio_use_custom_frames"))
 
+
 def knobChanged(node, knob):
     """Hide/show string knobs based on checkbox changes."""
 
+
     if knob.name() not in ["cio_use_scout_tasks", "cio_use_custom_frames"]:
         return
 
     value = bool(node.knob("cio_use_custom_frames").getValue())
     node.knob("cio_custom_frames").setVisible(value)
 
     value = bool(node.knob("cio_use_scout_tasks").getValue())
     node.knob("cio_scout_tasks").setVisible(value)
 
+
 def affector_knobs():
     return [
         "cio_chunk_size",
         "cio_custom_frames",
         "cio_use_custom_frames",
         "cio_scout_tasks",
         "cio_use_scout_tasks",
     ]
 
+
 def resolve(submitter, **kwargs):
+    main_sequence, scout_sequence = get_sequences(submitter)
 
     tasks = []
-
-    if submitter.input(0).Class() == "CopyCat":
-
-        scout_frames = "" #No need for scout frames
+    chunks = main_sequence.chunks()
+    for i, chunk in enumerate(chunks):
+        arg = "{}-{}".format(chunk.start, chunk.end)
+        submitter.knob("cio_range_args").setValue(arg)
         cmd = submitter.knob("cio_task").getValue()
 
-        for i in range(int(submitter.knob("cio_copycat_worker_size").getValue())):
-            tasks.append({"command": cmd, "frames": str(i)})
-
-    else:
-
-        main_sequence, scout_sequence = get_sequences(submitter)
-        scout_frames = ",".join([str(s) for s in scout_sequence or []])
-
-        chunks = main_sequence.chunks()
-        for i, chunk in enumerate(chunks):
-            arg = "{}-{}".format(chunk.start, chunk.end)
-            submitter.knob("cio_range_args").setValue(arg)
-            cmd = submitter.knob("cio_task").getValue()
-
-            tasks.append({"command": cmd, "frames": str(chunk)})
+        tasks.append({"command": cmd, "frames": str(chunk)})
 
-    task_data = {"tasks_data": tasks, "scout_frames": scout_frames}
+    return {"tasks_data": tasks, "scout_frames": ",".join([str(s) for s in scout_sequence or []])}
 
-    return task_data
 
 def get_sequences(submitter):
     """
     Create a cioseq.sequence for each of main frames and scout frames
 
     Returns:
         [tuple of sequence]: main frames and scout frames
```

## cionuke/components/software.py

```diff
@@ -1,12 +1,11 @@
 import nuke
 from ciocore import data as coredata
 from cionuke import const as k
 
-
 def build(submitter):
     """
     Build knobs to specify software configuration.
 
     Currently only Nuke version.
     """
     knob = nuke.Enumeration_Knob("cio_software", "Nuke version", [k.NOT_CONNECTED])
@@ -18,25 +17,96 @@
 def rebuild_menu(submitter, software_data):
 
     if not software_data:
         return
 
     items = sorted(software_data.supported_host_names())
     submitter.knob("cio_software").setValues(items or [k.NOT_CONNECTED])
-    if items:
-        submitter.knob("cio_software").setValue(items[-1])
+
 
 def resolve(submitter, **kwargs):
 
-    if (not coredata.valid()) or submitter.knob("cio_software").value() == k.NOT_CONNECTED:
+    if (not coredata.valid()) or submitter.knob(
+        "cio_software"
+    ).value() == k.NOT_CONNECTED:
         return {"software_package_ids": [k.INVALID]}
     tree_data = coredata.data()["software"]
     selected = submitter.knob("cio_software").value()
     package_ids = []
     package = tree_data.find_by_path(selected)
     if package:
         package_ids = [package["package_id"]]
     return {"software_package_ids": package_ids}
 
 
 def affector_knobs():
     return ["cio_software"]
+
+
+def _version_parts(version):
+    """
+    Split a version string into its parts.
+
+    Args:
+        version (str): The version string.
+
+    Returns:
+        tuple: A tuple of the version parts.
+    """
+    return tuple(map(int, version.split(".")))
+
+def find_closest_version(input_version, version_list):
+    """
+    Find the closest version in a list to the input version.
+
+    Args:
+        input_version (str): The version to compare.
+        version_list (list): List of versions to compare against.
+
+    Returns:
+        str: The closest version to the input version.
+
+    Logic is as follows:
+
+    If we find the same major and minor and patch version, we use it.
+    If we find the same major and minor we use the highest patch available, which might be lower than the target.
+    
+    If no major.minor match is found, we return the next highest version. This may be same-major, higher-minor, or it may be higher major.
+    
+    If no highter version is found, we use the highest version available.
+    """
+
+    the_version =   _version_parts(input_version.replace("v", "."))
+    
+    versions = [ _version_parts(v) for v in version_list]
+    
+    exact_major_minor_patch = [v for v in versions if v == the_version]
+    if exact_major_minor_patch:
+        return ".".join(map(str, exact_major_minor_patch[0]))
+    
+    same_major_minor = [v for v in versions if v[:2] == the_version[:2]]
+    if same_major_minor:
+        same_major_minor.sort()
+        return ".".join(map(str, same_major_minor[-1]))
+    
+    higher_major_minor = [v for v in versions if v[:2] > the_version[:2]]
+    if higher_major_minor:
+        higher_major_minor.sort()
+        return ".".join(map(str, higher_major_minor[0]))
+
+    # none the same major_minor and none higher. Just return the latest version.
+    versions.sort()
+    return ".".join(map(str, versions[-1]))
+
+def detect_version(submitter):
+    """
+    Detect the Nuke version and set the knob to the current version.
+    """
+    try:
+        current_version = nuke.NUKE_VERSION_STRING
+        versions = coredata.data()["software"].supported_host_names()
+        version_map = {str(v.split()[1]): str(v) for v in versions}
+        closest_version = find_closest_version(current_version, version_map.keys())
+        submitter.knob("cio_software").setValue(version_map[closest_version])
+    except Exception as ex:
+        # Don't do anything. It will default to lowest probably.
+        print("Could not detect Nuke version: {}".format(str(ex)))
```

## Comparing `cionuke-0.7.0b4.data/data/nuke/menu.py` & `cionuke-0.7.1b1.data/data/nuke/menu.py`

 * *Files identical despite different names*

## Comparing `cionuke-0.7.0b4.dist-info/METADATA` & `cionuke-0.7.1b1.dist-info/METADATA`

 * *Files 15% similar despite different names*

```diff
@@ -1,20 +1,20 @@
 Metadata-Version: 2.1
 Name: cionuke
-Version: 0.7.0b4
+Version: 0.7.1b1
 Summary: Nuke plugin for Conductor Cloud Rendering Platform.
 Home-page: https://github.com/ConductorTechnologies/cionuke
 Author: conductor
 Author-email: info@conductortech.com
 Classifier: Operating System :: OS Independent
 Classifier: Programming Language :: Python
 Classifier: Topic :: Multimedia :: Graphics :: 3D Rendering
 Description-Content-Type: text/markdown
 Requires-Dist: ciocore <8.0.0,>=7.0.2
-Requires-Dist: ciopath <1.0.0,>=0.1.7
+Requires-Dist: ciopath <2.0.0,>=1.1.2
 Requires-Dist: cioseq <1.0.0,>=0.1.14
 Requires-Dist: ciotemplate <1.0.0,>=0.1.2
 
 # Conductor for Nuke
 
 Nuke plugin submitter for the Conductor Cloud rendering service.
 
@@ -50,25 +50,23 @@
 
 
 ## License
 [MIT](https://choosealicense.com/licenses/mit)
 
 ## Changelog
 
-## 0.7.0-beta.3 -- 29 Feb 2024
-* Adds job polling so that the CopyCat node within Nuke receives live updates from the job
-  
-## 0.7.0-beta.3 -- 6 Feb 2024
-* Removes two instance limit for distributed CopyCat running remotely
+## Unreleased: 
 
-## 0.7.0-beta.2 -- 24 Jan 2024
-* Adds support for distributed CopyCat running remotely. Max 2 instances.
-
-## 0.7.0-beta.1 -- 06 Dec 2023
-* Adds support for CopyCat on single instance
+* 0.7.1-beta.1
+  * Adds validation that checks coredata resources (projects, inst types, software) before submission.
+  * The submitter now connects to coredata and detects the current version on creation of a new node.
+  * Extract the connect function because there was a py2 error (on Mac at least)
+  * Add ValidateConductorData validator to check for outdated Conductor data.
+  * No longer sets the nuke version when rebuilding the dropdown.
+  * Use PathList::real_files to expand folders for preview.
 
 ## Version:0.6.1 -- 11 Jan 2024
 * Fixes a missing import in 0.6.0
 
 ## Version:0.6.0 -- 06 Dec 2023
 * Adds a check to ensure that the output path exists and that no uploaded paths are inside the output path
 * Hides the preemptible checkbox for CW instances
```

## Comparing `cionuke-0.7.0b4.dist-info/RECORD` & `cionuke-0.7.1b1.dist-info/RECORD`

 * *Files 14% similar despite different names*

```diff
@@ -1,34 +1,31 @@
-cionuke/VERSION,sha256=y0_HQ04G8NSPf-yinp4Tjd8HmZSN5GJFxVzuqOxHkGE,12
+cionuke/VERSION,sha256=UhChEYX7TfnOnQcFdzEJfWaJ0wNlEcamVPkOM5SJ5Go,12
 cionuke/__about__.py,sha256=nTb4Xx0r9QtGROSFKgwDZ-Mr2LKjm2wVt1OkMQAkRAQ,241
 cionuke/__init__.py,sha256=tniDW1sH3MgTAKL1p6iwvnO9eXbBmFKKGx4S6QFbGvk,24
 cionuke/conductor_menu.py,sha256=TgTjH7XnRID0_xeRRA90zS4QMgbIDobCVhx1JIdcWDs,1541
-cionuke/const.py,sha256=-a58BpVBC4CWWWMaKHDX5qoKxA5YVvQ_mUDLMo242jI,671
-cionuke/copycat_poller.py,sha256=hYBmDJ9ESmQjoP7mDMV_L9DJZdrFLoK_kRSNs8ONuJo,6101
-cionuke/entry.py,sha256=B0uZf1pAQMc5mwAtj_DOqehOKtYV6xo5gsQwXkJR9WQ,3570
-cionuke/node_sync.py,sha256=oyOnAFq93CCKjvdfJ9PBOI8a1NTv0JSj4oOBKwoviq0,2593
+cionuke/connect.py,sha256=DKrwFxwRObqu-7-VRxezHPj6qX5Y0C7Vzm4Z06xn9vQ,1189
+cionuke/const.py,sha256=ZO06HOWH3QQsXZ3XkatsErS3lixQaPSQREUs10kfLYo,477
+cionuke/entry.py,sha256=8suTX3b_3ydy4XxMTUxSZII7vwZiQ0MGgg26aQ-PbHQ,3350
 cionuke/post_install.py,sha256=HWmI7-xfLoBjKbk7Iwo-nRDwxxM1vZqLBQzoaUdBhjU,2154
-cionuke/submit.py,sha256=USz04ZmobUuEU5GBNxAI3PsR2ChCkpPPslcP5nlnvbQ,10250
+cionuke/submit.py,sha256=UxtxJMIffcpvPgxG3MTWQHfVeWerA4a-xr2cfQhVO5g,8964
 cionuke/utils.py,sha256=z1g-bhuM-x1VpxR1A6kgw_i4_xbgisYCMCBaVdJoUrU,2452
-cionuke/validation.py,sha256=A6fKA5JK04aHWb75FT1YZJqThTAkQI9jdkRPRK3ZnHQ,3079
+cionuke/validation.py,sha256=qzw0u8KcbxOOzcVzfmmv08cBFI1lNmNmvNLoYf63HOc,5481
 cionuke/components/__init__.py,sha256=Nqnn8clbgv-5l0PgxcTOldg8mkMKrFn4TvPL-rYUUGg,1
-cionuke/components/actions.py,sha256=YeRdwvGbPrWbYEsdqGrNqtX1kHfMJ60jVnhrVgN6700,3117
-cionuke/components/advanced.py,sha256=VjfzWuRQgesGta18xKBfkwGqyIr9xL34GS2uGbSca5I,8908
-cionuke/components/assets.py,sha256=rUFtbjpBkZop29EDVXvU37pamuS_bmhqpwdWqbtft34,7713
+cionuke/components/actions.py,sha256=XGaX_v_zMc1dMfx7VsZ008Zz09ovi76-ZDjpD-A8eUA,2183
+cionuke/components/advanced.py,sha256=8fC3JHK4EbTxCr-HTHL7m5HlLa1UFKWSa9EVqc_SO6c,7014
+cionuke/components/assets.py,sha256=dN4xQ2NBq6k8cTAp0TGDL9TfjaJh0392MtgGbSLbblA,7998
 cionuke/components/autosave.py,sha256=1gJaUlTsjsfWWbP655gJMr6Oa9DeW4aRlZQuYuhTD4E,1155
 cionuke/components/conductor_knob.py,sha256=RQggE2UYMg1RCUaVAB5nlUSLl812Kr--M8Jjv-hcDVQ,1068
-cionuke/components/copy_cat.py,sha256=qL2Kf1yjQLl0pongcX16w5RlTJI79tsvsJIAlxfOzzs,4890
-cionuke/components/copycat_jobs.py,sha256=Pnfsk1Zu9PVdZVuV_Dja_R_I-4oM119f3DC-NscGAx4,2522
 cionuke/components/environment.py,sha256=b9C_2WEo6z3PWJPWprifnAbml5P9y6ibdIl_sgyWgGk,3663
-cionuke/components/frames.py,sha256=-EW6VcXfIVU6uVvJSFx-bbPpbMJPlJP9ICfQwqPFhT8,3843
+cionuke/components/frames.py,sha256=sdNVk23HHi7gQQ1rDqC7RVfPw3ntlWzJSryGDk1Aw1g,3446
 cionuke/components/instance_type.py,sha256=bj8EyO1vvmhe2V8x_VJeUcV5Fmq2RlsNAFisPJXRkyA,2713
 cionuke/components/metadata.py,sha256=C2q-dowOfHsesuKDfsYckCT-vbK4WhguSEOsCOuOtBI,2473
 cionuke/components/preview.py,sha256=bR2WwCYVNd0PAAYdp5HYAv8bq7M-2XGiVvZWzwITCz0,3154
 cionuke/components/project.py,sha256=xR6NPen_mHbpRsoZcKNGZ_luQmoK66jcTxq3HVP9d7o,768
-cionuke/components/software.py,sha256=_6QiA5ESPWLgDaZuRA65SoDiRQLHycWXT2rHeYeDjI0,1198
+cionuke/components/software.py,sha256=9mjAYx2XdBHBiqMNS6JRlUoteB9wk-oyOXOG-NeKmO8,3629
 cionuke/components/title.py,sha256=Siwp9NiuFwOr6fVarWVVT6iFlMWf798o724aPv287Yk,593
 cionuke/icons/ConductorConnect_18x18.png,sha256=DDsRg0l471tFQjJ3UYD2s4J6fvtUNjMCw5mlLqmQ30c,939
 cionuke/icons/ConductorConnect_24x24.png,sha256=k-upvseBm7JW7iUHInTVIQmrNSCoLQzWQUQtubu433M,1254
 cionuke/icons/ConductorConnect_36x36.png,sha256=gJFxuG2oZj9EqvYQf-x2Yeh4L-cRaumYjMl84heoPSg,1815
 cionuke/icons/ConductorConnect_48x48.png,sha256=RJYA5xfkQadsPhLqgr49wvQYgqNZnBzcMfu5H85XUVU,2457
 cionuke/icons/ConductorError_18x18.png,sha256=x-WnL6eKcUK4ILt-45PpDrwruZnuRpQFBVhZNx9Hfmo,1137
 cionuke/icons/ConductorError_24x24.png,sha256=2RXEVtg1hh_gkmWwbWfbWAjqN8Z80yTb50HQkmYFybU,1553
@@ -54,12 +51,12 @@
 cionuke/icons/ConductorWarning_24x24.png,sha256=cOT1e2Af5mEgmgeQu6E3WHuaHeOLbKy-vE4I-5OjN1E,1177
 cionuke/icons/ConductorWarning_36x36.png,sha256=q653PCnZmLCok1lW0kpHRyY7JHiSkfEUq0S0yI_ikNQ,1668
 cionuke/icons/ConductorWarning_48x48.png,sha256=vqidcjO_UHuDg9LZ8TNK0SOnvJgCOB159tQBG3xUO5U,2204
 cionuke/widgets/__init__.py,sha256=Nqnn8clbgv-5l0PgxcTOldg8mkMKrFn4TvPL-rYUUGg,1
 cionuke/widgets/buttoned_scroll_dialog.py,sha256=EMD2mwJtr1vJnwlyatlAE6SJha6DCOfdVHUQWUY46tc,2067
 cionuke/widgets/key_value_grp.py,sha256=lltzTTzLcMKuQlEatYQMVLqwHDVeB12IPsQz3VClPKo,8037
 cionuke/widgets/notice_grp.py,sha256=14DRFGPxNZe4nmtQJNZl_4-VHAWgVmPKu49ax9hPeGc,1729
-cionuke-0.7.0b4.data/data/nuke/menu.py,sha256=yHgWObZJ_-1eiveu1l5-ocl2XdSmJh_aeHrEt35aZQI,1288
-cionuke-0.7.0b4.dist-info/METADATA,sha256=ZdizvvrfhiF1haRKVcy_iAUYDfWc3XNjsp207fIysv0,5406
-cionuke-0.7.0b4.dist-info/WHEEL,sha256=iYlv5fX357PQyRT2o6tw1bN-YcKFFHKqB_LwHO5wP-g,110
-cionuke-0.7.0b4.dist-info/top_level.txt,sha256=i1Qvu68r5eoG933Sueeydwk2I4avZE4Px2ggK9R0uhA,8
-cionuke-0.7.0b4.dist-info/RECORD,,
+cionuke-0.7.1b1.data/data/nuke/menu.py,sha256=yHgWObZJ_-1eiveu1l5-ocl2XdSmJh_aeHrEt35aZQI,1288
+cionuke-0.7.1b1.dist-info/METADATA,sha256=5VgfFHvZUjo3bLeBGyzjqUehBmu_BNZyVr-RffwBVc0,5519
+cionuke-0.7.1b1.dist-info/WHEEL,sha256=DZajD4pwLWue70CAfc7YaxT1wLUciNBvN_TTcvXpltE,110
+cionuke-0.7.1b1.dist-info/top_level.txt,sha256=i1Qvu68r5eoG933Sueeydwk2I4avZE4Px2ggK9R0uhA,8
+cionuke-0.7.1b1.dist-info/RECORD,,
```

