# Comparing `tmp/fluidfoam-0.2.5-py3-none-any.whl.zip` & `tmp/fluidfoam-0.2.6-py3-none-any.whl.zip`

## zipinfo {}

```diff
@@ -1,22 +1,22 @@
-Zip file size: 36740 bytes, number of entries: 20
--rw-r--r--  2.0 unx      908 b- defN 23-May-14 18:03 fluidfoam/__init__.py
--rw-r--r--  2.0 unx      343 b- defN 23-May-14 18:03 fluidfoam/_version.py
--rw-r--r--  2.0 unx     2785 b- defN 23-May-14 18:03 fluidfoam/meshdesign.py
--rw-r--r--  2.0 unx    11968 b- defN 23-May-14 18:03 fluidfoam/meshvisu.py
--rw-r--r--  2.0 unx     6659 b- defN 23-May-14 18:03 fluidfoam/openfoamsimu.py
--rw-r--r--  2.0 unx    15156 b- defN 23-May-14 18:03 fluidfoam/processing1d.py
--rw-r--r--  2.0 unx    37751 b- defN 23-May-14 18:03 fluidfoam/readof.py
--rw-r--r--  2.0 unx     7402 b- defN 23-May-14 18:03 fluidfoam/readpostpro.py
--rw-r--r--  2.0 unx      967 b- defN 23-May-14 18:03 fluidfoam/test_create1dprofil.py
--rw-r--r--  2.0 unx      465 b- defN 23-May-14 18:03 fluidfoam/test_create1dprofilDFSEM.py
--rw-r--r--  2.0 unx      588 b- defN 23-May-14 18:03 fluidfoam/test_meshdesign.py
--rw-r--r--  2.0 unx      940 b- defN 23-May-14 18:03 fluidfoam/test_meshvisu.py
--rw-r--r--  2.0 unx      378 b- defN 23-May-14 18:03 fluidfoam/test_openfoamsimu.py
--rw-r--r--  2.0 unx     3359 b- defN 23-May-14 18:03 fluidfoam/test_read.py
--rw-r--r--  2.0 unx      930 b- defN 23-May-14 18:03 fluidfoam/test_readpostpro.py
--rw-r--r--  2.0 unx    35148 b- defN 23-May-14 18:04 fluidfoam-0.2.5.dist-info/LICENSE.txt
--rw-r--r--  2.0 unx     4065 b- defN 23-May-14 18:04 fluidfoam-0.2.5.dist-info/METADATA
--rw-r--r--  2.0 unx       92 b- defN 23-May-14 18:04 fluidfoam-0.2.5.dist-info/WHEEL
--rw-r--r--  2.0 unx       10 b- defN 23-May-14 18:04 fluidfoam-0.2.5.dist-info/top_level.txt
--rw-rw-r--  2.0 unx     1634 b- defN 23-May-14 18:04 fluidfoam-0.2.5.dist-info/RECORD
-20 files, 131548 bytes uncompressed, 34086 bytes compressed:  74.1%
+Zip file size: 37478 bytes, number of entries: 20
+-rw-r--r--  2.0 unx      920 b- defN 24-Apr-16 11:34 fluidfoam/__init__.py
+-rw-r--r--  2.0 unx      343 b- defN 24-Apr-16 11:34 fluidfoam/_version.py
+-rw-r--r--  2.0 unx     2785 b- defN 24-Apr-16 11:34 fluidfoam/meshdesign.py
+-rw-r--r--  2.0 unx    11968 b- defN 24-Apr-16 11:34 fluidfoam/meshvisu.py
+-rw-r--r--  2.0 unx     6659 b- defN 24-Apr-16 11:34 fluidfoam/openfoamsimu.py
+-rw-r--r--  2.0 unx    15156 b- defN 24-Apr-16 11:34 fluidfoam/processing1d.py
+-rw-r--r--  2.0 unx    45813 b- defN 24-Apr-16 11:34 fluidfoam/readof.py
+-rw-r--r--  2.0 unx     7402 b- defN 24-Apr-16 11:34 fluidfoam/readpostpro.py
+-rw-r--r--  2.0 unx      967 b- defN 24-Apr-16 11:34 fluidfoam/test_create1dprofil.py
+-rw-r--r--  2.0 unx      465 b- defN 24-Apr-16 11:34 fluidfoam/test_create1dprofilDFSEM.py
+-rw-r--r--  2.0 unx      588 b- defN 24-Apr-16 11:34 fluidfoam/test_meshdesign.py
+-rw-r--r--  2.0 unx      940 b- defN 24-Apr-16 11:34 fluidfoam/test_meshvisu.py
+-rw-r--r--  2.0 unx      378 b- defN 24-Apr-16 11:34 fluidfoam/test_openfoamsimu.py
+-rw-r--r--  2.0 unx     3359 b- defN 24-Apr-16 11:34 fluidfoam/test_read.py
+-rw-r--r--  2.0 unx      930 b- defN 24-Apr-16 11:34 fluidfoam/test_readpostpro.py
+-rw-r--r--  2.0 unx    35148 b- defN 24-Apr-16 11:34 fluidfoam-0.2.6.dist-info/LICENSE.txt
+-rw-r--r--  2.0 unx     4059 b- defN 24-Apr-16 11:34 fluidfoam-0.2.6.dist-info/METADATA
+-rw-r--r--  2.0 unx       92 b- defN 24-Apr-16 11:34 fluidfoam-0.2.6.dist-info/WHEEL
+-rw-r--r--  2.0 unx       10 b- defN 24-Apr-16 11:34 fluidfoam-0.2.6.dist-info/top_level.txt
+-rw-rw-r--  2.0 unx     1634 b- defN 24-Apr-16 11:34 fluidfoam-0.2.6.dist-info/RECORD
+20 files, 139616 bytes uncompressed, 34824 bytes compressed:  75.1%
```

## zipnote {}

```diff
@@ -39,23 +39,23 @@
 
 Filename: fluidfoam/test_read.py
 Comment: 
 
 Filename: fluidfoam/test_readpostpro.py
 Comment: 
 
-Filename: fluidfoam-0.2.5.dist-info/LICENSE.txt
+Filename: fluidfoam-0.2.6.dist-info/LICENSE.txt
 Comment: 
 
-Filename: fluidfoam-0.2.5.dist-info/METADATA
+Filename: fluidfoam-0.2.6.dist-info/METADATA
 Comment: 
 
-Filename: fluidfoam-0.2.5.dist-info/WHEEL
+Filename: fluidfoam-0.2.6.dist-info/WHEEL
 Comment: 
 
-Filename: fluidfoam-0.2.5.dist-info/top_level.txt
+Filename: fluidfoam-0.2.6.dist-info/top_level.txt
 Comment: 
 
-Filename: fluidfoam-0.2.5.dist-info/RECORD
+Filename: fluidfoam-0.2.6.dist-info/RECORD
 Comment: 
 
 Zip file comment:
```

## fluidfoam/__init__.py

```diff
@@ -1,11 +1,11 @@
 import warnings
 
 from fluidfoam.readof import readscalar, readvector, readtensor
-from fluidfoam.readof import readsymmtensor, readfield, readmesh
+from fluidfoam.readof import readsymmtensor, readfield, readmesh, getVolumes
 from fluidfoam.readof import typefield, OpenFoamFile
 from fluidfoam.processing1d import create1dprofil, read1dprofil
 from fluidfoam.processing1d import create1dprofil_spe, plot1dprofil
 from fluidfoam.processing1d import create1dprofilDFSEM, read1dprofilDFSEM
 from fluidfoam.meshdesign import getgz, getdzs
 from fluidfoam.readpostpro import readforce, readprobes
 from fluidfoam.meshvisu import MeshVisu
```

## fluidfoam/_version.py

```diff
@@ -7,8 +7,8 @@
 
 http://en.wikipedia.org/wiki/Software_versioning
 http://legacy.python.org/dev/peps/pep-0386/
 
 'a' or 'alpha' means alpha version (internal testing),
 'b' or 'beta' means beta version (external testing).
 """
-__version__ = "0.2.5"
+__version__ = "0.2.6"
```

## fluidfoam/readof.py

```diff
@@ -10,23 +10,26 @@
 
 .. autofunction:: readvector
 
 .. autofunction:: readsymmtensor
 
 .. autofunction:: readtensor
 
+.. autofunction:: getVolumes
+
 .. autofunction:: typefield
 
 """
 
 
 import os
 import gzip
 import struct
 import numpy as np
+import scipy.spatial as ss
 
 # define color
 W = '\033[0m'  # white (normal)
 R = '\033[31m'  # red
 G = '\033[32m'  # green
 O = '\033[33m'  # orange
 B = '\033[34m'  # blue
@@ -109,14 +112,18 @@
 
         try:
             self.is_ascii = self.header[b"format"] == b"ascii"
             self.noheader = False
         except KeyError:
             self.is_ascii = True
             self.noheader = True
+        try:
+            self.is_SP    = b"scalar=32" in self.header[b"arch"]
+        except KeyError:
+            self.is_SP    = False
 
         for line in self.lines_stripped:
             if line.startswith(b"dimensions"):
                 tmp = (line.split(b"[")[1]).split(b"]")[0]
                 self.dimensions = eval(b", ".join(tmp.split()))
 
         self.boundary = self._parse_session(b"boundaryField")
@@ -282,20 +289,28 @@
                 nb_numbers = nb_pts
             elif self.type_data == "vector":
                 nb_numbers = 3 * nb_pts
             elif self.type_data == "symmtensor":
                 nb_numbers = 6 * nb_pts
             elif self.type_data == "tensor":
                 nb_numbers = 9 * nb_pts
-            self.values = np.array(
-                struct.unpack(
-                    "{}d".format(nb_numbers),
-                    data[: nb_numbers * struct.calcsize("d")],
+            if self.is_SP:
+                self.values = np.array(
+                    struct.unpack(
+                        "{}f".format(nb_numbers),
+                        data[: nb_numbers * struct.calcsize("f")],
+                    )
+                )
+            else:
+                self.values = np.array(
+                    struct.unpack(
+                        "{}d".format(nb_numbers),
+                        data[: nb_numbers * struct.calcsize("d")],
+                    )
                 )
-            )
         else:
             if self.type_data == "scalar":
                 self.values = np.array(
                     [float(s) for s in data.strip().split(b"\n")[:nb_pts]]
                 )
             elif self.type_data in ("vector", "tensor", "symmtensor"):
                 lines = data.split(b";")[0].split(b"\n(")
@@ -379,20 +394,28 @@
                 nb_numbers = nb_pts
             elif self.type_data == "vector":
                 nb_numbers = 3 * nb_pts
             elif self.type_data == "symmtensor":
                 nb_numbers = 6 * nb_pts
             elif self.type_data == "tensor":
                 nb_numbers = 9 * nb_pts
-            values = np.array(
-                struct.unpack(
-                    "{}d".format(nb_numbers),
-                    data[: nb_numbers * struct.calcsize("d")],
+            if self.is_SP:
+                values = np.array(
+                    struct.unpack(
+                        "{}f".format(nb_numbers),
+                        data[: nb_numbers * struct.calcsize("f")],
+                    )
+                )
+            else:
+                values = np.array(
+                    struct.unpack(
+                        "{}d".format(nb_numbers),
+                        data[: nb_numbers * struct.calcsize("d")],
+                    )
                 )
-            )
         else:
             if self.type_data == "scalar":
                 values = np.array(
                     [float(s) for s in data.strip().split(b"\n")[:nb_pts]]
                 )
             elif self.type_data in ("vector", "tensor", "symmtensor"):
                 lines = data.split(b"\n(")
@@ -491,20 +514,28 @@
         data = self.content.split(line, 1)[1]
 
         self.type_data = self.header[b"class"]
 
         if not self.is_ascii:
             nb_numbers = 3 * self.nb_pts
             data = b"\n(".join(data.split(b"\n(")[1:])
-            self.values = np.array(
-                struct.unpack(
-                    "{}d".format(nb_numbers),
-                    data[: nb_numbers * struct.calcsize("d")],
+            if self.is_SP:
+                self.values = np.array(
+                    struct.unpack(
+                        "{}f".format(nb_numbers),
+                        data[: nb_numbers * struct.calcsize("f")],
+                    )
+                )
+            else:
+                self.values = np.array(
+                    struct.unpack(
+                        "{}d".format(nb_numbers),
+                        data[: nb_numbers * struct.calcsize("d")],
+                    )
                 )
-            )
         else:
             lines = data.split(b"\n(")
             lines = [line.split(b")")[0] for line in lines]
             data = b" ".join(lines).strip()
             self.values = np.array([float(s) for s in data.split()])
 
         self.values = np.around(self.values, decimals=precision)
@@ -1070,29 +1101,31 @@
             npts = int(facefile.faces[id0 + i]["npts"])
             id_pts = np.zeros(npts, dtype=int)
             id_pts[0:npts] = facefile.faces[id0 + i]["id_pts"][0:npts]
             xs[i] = np.mean(pointfile.values_x[id_pts[0:npts]])
             ys[i] = np.mean(pointfile.values_y[id_pts[0:npts]])
             zs[i] = np.mean(pointfile.values_z[id_pts[0:npts]])
     else:
-        owner = OpenFoamFile(
-            path + meshpath, name="owner", verbose=verbose
-        )
-        nmesh = owner.nb_cell
         if (time_name is None and region is None
                 and os.path.exists(os.path.join(path, "constant/C"))):
             xs, ys, zs = readvector(
                 path, "constant", "C", precision=precision, verbose=verbose
             )
+            nmesh = np.size(xs)
         elif (time_name is not None and region is None
               and os.path.exists(_make_path(path, time_name, "C"))):
             xs, ys, zs = readvector(
                 path, time_name, "C", precision=precision, verbose=verbose
             )
+            nmesh = np.size(xs)
         else:
+            owner = OpenFoamFile(
+                path + meshpath, name="owner", verbose=verbose
+            )
+            nmesh = owner.nb_cell
             facefile = OpenFoamFile(
                 path + meshpath, name="faces", verbose=verbose
             )
             if time_name is not None and region is None:
                 pointfile = OpenFoamFile(
                     path=path,
                     time_name=time_name,
@@ -1159,14 +1192,204 @@
         xs = xs[setsfile.values]
         ys = ys[setsfile.values]
         zs = zs[setsfile.values]
 
     return xs, ys, zs
 
 
+
+
+
+def getVolumes(
+    path,
+    time_name=None,
+    structured=False,
+    boundary=None,
+    sets=None,
+    region=None,
+    order="F",
+    precision=15,
+    verbose=True,
+    box=None
+):
+    """
+    Reads OpenFoam mesh and returns the cell centroids and cell volumes
+    of a given box.
+
+    Args:
+        path: str\n
+        time_name: str ('latestTime' is supported)\n
+        structured: False or True\n
+        boundary: None or str\n
+        sets: None or str\n
+        region: None or str\n
+        order: "F" (default) or "C" \n
+        precision : Number of decimal places to round to (default: 15)\n
+        verbose : True or False (default: True)
+        box : tuple of box's dimension: ((xmin, ymin, zmin), (xmax, ymax, zmax))\n
+               (if None, includes the whole mesh)\n
+
+    Returns:
+        array: two arrays that contain the cell centroids and cell volumes
+
+    A way you might use me is:\n
+        centroidList,vol = fluidfoam.getVolumes('path_of_OpenFoam_case')
+        So centroidList and vol are the cell centroid and cell volume arrays.
+
+
+    """
+
+    if time_name == "0":
+        time_name = None
+    if region is None:
+        meshpath = "/constant/polyMesh/"
+    else:
+        meshpath = "/constant/"+region+"/polyMesh/"
+    if not os.path.exists(path+meshpath):
+        raise ValueError(
+            "No ", meshpath, " directory in ",
+            path,
+            " Please verify the directory of your case.",
+        )
+
+    if boundary is not None:
+        facefile = OpenFoamFile(
+            path + meshpath, name="faces", verbose=verbose
+        )
+        if time_name is not None:
+            pointfile = OpenFoamFile(
+                path=path,
+                time_name=time_name,
+                name="polyMesh/points",
+                precision=precision,
+                verbose=verbose
+            )
+        else:
+            pointfile = OpenFoamFile(
+                path + meshpath,
+                name="points",
+                precision=precision,
+                verbose=verbose
+            )
+        bounfile = OpenFoamFile(
+            path + meshpath,
+            name="boundary",
+            verbose=verbose
+        )
+        id0 = int(bounfile.boundaryface[str.encode(boundary)][b"startFace"])
+        nmesh = int(bounfile.boundaryface[str.encode(boundary)][b"nFaces"])
+
+        xs = np.empty(nmesh, dtype=float)
+        ys = np.empty(nmesh, dtype=float)
+        zs = np.empty(nmesh, dtype=float)
+
+        for i in range(nmesh):
+            npts = int(facefile.faces[id0 + i]["npts"])
+            id_pts = np.zeros(npts, dtype=int)
+            id_pts[0:npts] = facefile.faces[id0 + i]["id_pts"][0:npts]
+            xs[i] = np.mean(pointfile.values_x[id_pts[0:npts]])
+            ys[i] = np.mean(pointfile.values_y[id_pts[0:npts]])
+            zs[i] = np.mean(pointfile.values_z[id_pts[0:npts]])
+    else:
+        if (time_name is None and region is None
+                and os.path.exists(os.path.join(path, "constant/C"))):
+            xs, ys, zs = readvector(
+                path, "constant", "C", precision=precision, verbose=verbose
+            )
+            nmesh = np.size(xs)
+        elif (time_name is not None and region is None
+              and os.path.exists(_make_path(path, time_name, "C"))):
+            xs, ys, zs = readvector(
+                path, time_name, "C", precision=precision, verbose=verbose
+            )
+            nmesh = np.size(xs)
+        else:
+            owner = OpenFoamFile(
+                path + meshpath, name="owner", verbose=verbose
+            )
+            nmesh = owner.nb_cell
+            facefile = OpenFoamFile(
+                path + meshpath, name="faces", verbose=verbose
+            )
+            if time_name is not None and region is None:
+                pointfile = OpenFoamFile(
+                    path=path,
+                    time_name=time_name,
+                    name="polyMesh/points",
+                    precision=precision,
+                    verbose=verbose
+                )
+            else:
+                pointfile = OpenFoamFile(
+                    path + meshpath,
+                    name="points",
+                    precision=precision,
+                    verbose=verbose
+                )
+            neigh = OpenFoamFile(
+                path + meshpath, name="neighbour", verbose=verbose
+            )
+            xs = np.empty(owner.nb_cell, dtype=float)
+            ys = np.empty(owner.nb_cell, dtype=float)
+            zs = np.empty(owner.nb_cell, dtype=float)
+            face = {}
+            for i in range(neigh.nb_faces):
+                if not neigh.values[i] in face:
+                    face[neigh.values[i]] = list()
+                face[neigh.values[i]].append(facefile.faces[i]["id_pts"][:])
+            for i in range(owner.nb_faces):
+                if not owner.values[i] in face:
+                    face[owner.values[i]] = list()
+                face[owner.values[i]].append(facefile.faces[i]["id_pts"][:])
+
+            if box != None:
+               # box = ((xmin, ymin, zmin), (xmax, ymax, zmax))
+                if len(box[0]) != 3 or len(box[1]) != 3:
+                    raise ValueError("box mins and maxs must be float tuples of lenght 3")
+            else:
+                minx = np.min(pointfile.values_x)
+                miny = np.min(pointfile.values_y)
+                minz = np.min(pointfile.values_z)
+                maxx = np.max(pointfile.values_x)
+                maxy = np.max(pointfile.values_y)
+                maxz = np.max(pointfile.values_z)
+                box = ((minx, miny, minz), (maxx, maxy, maxz))
+
+            centroidCell = np.empty((0,3), dtype=float)
+            VolCell_all = np.empty(owner.nb_cell, dtype=float)
+
+            for i in range(owner.nb_cell):
+                xs[i] = np.mean(
+                    pointfile.values_x[np.unique(np.concatenate(face[i])[:])]
+                )
+                ys[i] = np.mean(
+                    pointfile.values_y[np.unique(np.concatenate(face[i])[:])]
+                )
+                zs[i] = np.mean(
+                    pointfile.values_z[np.unique(np.concatenate(face[i])[:])]
+                )
+
+                if box[0][0] < xs[i] < box[1][0] and box[0][1] < ys[i] < box[1][1] and box[0][2] < zs[i] < box[1][2]:
+                    pointsCell=[]
+                    for k in zip(np.unique(np.concatenate(face[i])[:])):
+                        pointsCell.append([pointfile.values_x[k],pointfile.values_y[k],pointfile.values_z[k]])
+
+                    # Add 3D elements into the empty array
+                    element = np.array([xs[i], ys[i], zs[i]])
+                    centroidCell = np.append(centroidCell, [element], axis=0)
+                    VolCell_all[i]=ss.ConvexHull(pointsCell).volume
+
+            # Exclude null values from the array
+            VolCell = VolCell_all[VolCell_all != 0]
+
+    return centroidCell,VolCell
+
+
+
+
 if __name__ == "__main__":
 
     dirs = ["ascii", "asciigz", "bin", "bingz"]
 
     for d in dirs:
         rep = os.path.join(os.path.dirname(__file__), "../output_samples/", d)
```

## Comparing `fluidfoam-0.2.5.dist-info/LICENSE.txt` & `fluidfoam-0.2.6.dist-info/LICENSE.txt`

 * *Files identical despite different names*

## Comparing `fluidfoam-0.2.5.dist-info/METADATA` & `fluidfoam-0.2.6.dist-info/METADATA`

 * *Files 2% similar despite different names*

```diff
@@ -1,10 +1,10 @@
 Metadata-Version: 2.1
 Name: fluidfoam
-Version: 0.2.5
+Version: 0.2.6
 Summary: Openfoam PostProcessing Python Tools
 Home-page: http://legi.grenoble-inp.fr
 Author: Cyrille Bonamy
 Author-email: cyrille.bonamy@univ-grenoble-alpes.fr
 License: GPLv2
 Keywords: Openfoam,postprocessing,python,CFD
 Classifier: Development Status :: 5 - Production/Stable
@@ -15,17 +15,17 @@
 Classifier: Programming Language :: Python
 Classifier: Programming Language :: Python :: 3
 Classifier: Programming Language :: Python :: 3.8
 Classifier: Programming Language :: Python :: 3.9
 Classifier: Programming Language :: Python :: 3.10
 Description-Content-Type: text/markdown
 License-File: LICENSE.txt
-Requires-Dist: numpy (>=1.11)
-Requires-Dist: scipy (>=1.7.3)
-Requires-Dist: matplotlib (>=1.5)
+Requires-Dist: numpy >=1.11
+Requires-Dist: scipy >=1.7.3
+Requires-Dist: matplotlib >=1.5
 Requires-Dist: netCDF4
 
 ========
 fluidfoam
 ========
 
 |release| |docs| |Github-action| |coverage|
@@ -48,16 +48,16 @@
 
 The fluidfoam package provides Python classes useful to perform some plot with OpenFoam data.
 
 What is this repository for?
 ----------------------------
 
 * Openfoam Tools
-* Version : 0.2.5
-* Supported OpenFoam Versions : 2.4.0, 4.1 to 9, v1712plus to v2212plus
+* Version : 0.2.6
+* Supported OpenFoam Versions : 2.4.0, 4.1 to 9, v1712plus to v2312plus
 * Supported Python Versions : >= 3.8
 
 Documentation and Examples
 --------------------------
 
 `http://fluidfoam.readthedocs.org
 <http://fluidfoam.readthedocs.org>`_
```

## Comparing `fluidfoam-0.2.5.dist-info/RECORD` & `fluidfoam-0.2.6.dist-info/RECORD`

 * *Files 18% similar despite different names*

```diff
@@ -1,20 +1,20 @@
-fluidfoam/__init__.py,sha256=if3AhmW8Lmaec08R9ntBqSZey4zwLH1_SGYjFz1Jd34,908
-fluidfoam/_version.py,sha256=LSB3oQLYUhdT3MyGxtaVnlhCAp87BpEHkkWK1_myfuE,343
+fluidfoam/__init__.py,sha256=VrQz_bu8YNS7ut6Gsq7qDD4_k4KAlupEuVR0XWSG3Ic,920
+fluidfoam/_version.py,sha256=kKhfFsOWC9YiyGZE1ZSMfSqDb7qaoVKfcUDcUHj_9f4,343
 fluidfoam/meshdesign.py,sha256=3l4O4wUmFQus98IMOKy5vvrYfM2cvlk1uCzTpuUz1DU,2785
 fluidfoam/meshvisu.py,sha256=Bg4n5S3MGWCg3Ri9_l5NURFzbhydCadN7GV1ZySFy64,11968
 fluidfoam/openfoamsimu.py,sha256=ahu117tHIPxIeuh8b7FqAiwYpljulWGLGRUKMsTu2Ms,6659
 fluidfoam/processing1d.py,sha256=7PfKFLktCh48TX5wwaj0_0T23rZerE4-TrLsgC6WvgQ,15156
-fluidfoam/readof.py,sha256=mmlBLk3nmRcHZxUmq9qx3YFCur2I7WmM0oHCKOfbYes,37751
+fluidfoam/readof.py,sha256=ZT23SyHQx55FXKggzraSfOQbA-Pj0NvXb0DQKqRxBVQ,45813
 fluidfoam/readpostpro.py,sha256=zNqebRvcr7kt-LfxT8Ecy6uCW9ZCIxx8Hw25p0Ac9Vo,7402
 fluidfoam/test_create1dprofil.py,sha256=gXgFx0yWzcbSC0y1hbI6lkoF3C-Yj8ncYtSi3vGL4tg,967
 fluidfoam/test_create1dprofilDFSEM.py,sha256=ZxBD7rsK1oha7pdKdJkoigYMPurKJ9wv4vQ-KmPXpyU,465
 fluidfoam/test_meshdesign.py,sha256=JOINfD5g9buaY8FPDZJbSF_4y1zFq64xPoZzZQDIE90,588
 fluidfoam/test_meshvisu.py,sha256=epT0Fp6cXETj6cRlV91cTZVlWL1fo-Lvo4d83mSn-B0,940
 fluidfoam/test_openfoamsimu.py,sha256=ZXjNl89hfx6gfXuBIX5n5VEH6rbsNmUoyaK_MBtck0k,378
 fluidfoam/test_read.py,sha256=16w58dGj4Z5xyc2VsUpf_z9LUZoonnezrUx1LjQSRvo,3359
 fluidfoam/test_readpostpro.py,sha256=sXEVxYyF71lbrb9_eDlKe69WC3wt3pPgWY06S9wMMIs,930
-fluidfoam-0.2.5.dist-info/LICENSE.txt,sha256=CuBIWlvTemPmNgNZZBfk6w5lMzT6bH-TLKOg6F1K8ic,35148
-fluidfoam-0.2.5.dist-info/METADATA,sha256=70OW0nowzwB2W92Ad3VsExTS8d-9sq83-AbfkdZNyaA,4065
-fluidfoam-0.2.5.dist-info/WHEEL,sha256=pkctZYzUS4AYVn6dJ-7367OJZivF2e8RA9b_ZBjif18,92
-fluidfoam-0.2.5.dist-info/top_level.txt,sha256=HEhuraZVHEEN2hk-yhX9KKUKui7HePGPU-EKM6NGxnA,10
-fluidfoam-0.2.5.dist-info/RECORD,,
+fluidfoam-0.2.6.dist-info/LICENSE.txt,sha256=CuBIWlvTemPmNgNZZBfk6w5lMzT6bH-TLKOg6F1K8ic,35148
+fluidfoam-0.2.6.dist-info/METADATA,sha256=HE5Zoo56DUCWV7WJ3PZOHN_RTxKRDHBmaIHkEceL1hQ,4059
+fluidfoam-0.2.6.dist-info/WHEEL,sha256=GJ7t_kWBFywbagK5eo9IoUwLW6oyOeTKmQ-9iHFVNxQ,92
+fluidfoam-0.2.6.dist-info/top_level.txt,sha256=HEhuraZVHEEN2hk-yhX9KKUKui7HePGPU-EKM6NGxnA,10
+fluidfoam-0.2.6.dist-info/RECORD,,
```

